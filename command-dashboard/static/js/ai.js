/**
 * ai.js — AI 推薦層 frozen stub（C1-F, v3.1.0 接入點）
 *
 * 此模組為佔位 stub，尚未實作。
 * 實際邏輯留待 C5-C（AI 預測層）+ C5-E（AI 資安）時接入。
 *
 * 接入時需實作：
 *   - POST /api/ai/recommend（讀當下 COP 狀態）
 *   - GET /api/ai/report/{exercise_id}
 *   - prompt injection 防護（C5-E）
 *   - output sandbox（C5-E）
 *
 * ⚠️  Do NOT implement any logic in this file.
 *     Any implementation belongs to the C5-C task card.
 */

console.warn('[ICS_DMAS] ai.js stub - not implemented');

export default Object.freeze({
  __stub: true,
});
