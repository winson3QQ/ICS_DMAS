[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --tb=short
    --strict-markers
    -q
markers =
    unit: 純函式測試，不碰 DB 或網路
    integration: 直接操作 SQLite，不起 HTTP server
    api: 透過 FastAPI TestClient 發 HTTP 請求
